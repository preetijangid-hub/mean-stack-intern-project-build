const express=require("express"); const cors=require("cors"); const authRoutes=require("./routes/auth.routes"); const taskRoutes=require("./routes/task.routes"); const app=express(); const origins=["http://localhost:4200",process.env.CLIENT_ORIGIN].filter(Boolean); app.use(cors({origin:(o,cb)=>!o||origins.includes(o)?cb(null,true):cb(new Error("CORS origin not allowed")),methods:["GET","POST","PUT","DELETE","OPTIONS"],allowedHeaders:["Content-Type","Authorization"]})); app.use(express.json()); app.get("/",(q,s)=>s.json({success:true,message:"TaskFlow Day 27 API is running"})); app.get("/api/health",(q,s)=>s.json({success:true,message:"API healthy"})); app.use("/api/auth",authRoutes); app.use("/api/tasks",taskRoutes); app.use((q,s)=>s.status(404).json({success:false,message:"Route not found"})); app.use((e,q,s,n)=>{console.error(e);s.status(500).json({success:false,message:"Internal server error"})}); module.exports=app;
