import{Injectable,inject}from"@angular/core";import{HttpClient}from"@angular/common/http";import{tap}from"rxjs";import{environment}from"../../environments/environment";export interface AuthUser{id:string;name:string;email:string}export interface AuthResponse{success:boolean;message:string;token:string;user?:AuthUser}@Injectable({providedIn:"root"})export class AuthService{private http=inject(HttpClient);private url=`${environment.apiUrl}/auth`;private readonly tokenKey="token";register(name:string,email:string,password:string){return this.http.post<AuthResponse>(`${this.url}/register`,{name,email,password}).pipe(tap(r=>{this.saveToken(r.token);this.saveUser(r.user)}))}login(email:string,password:string){return this.http.post<AuthResponse>(`${this.url}/login`,{email,password}).pipe(tap(r=>{this.saveToken(r.token);this.saveUser(r.user)}))}saveToken(t:string){localStorage.setItem(this.tokenKey,t)}getToken(){return localStorage.getItem(this.tokenKey)}getUser():AuthUser|null{const value=localStorage.getItem("user");if(!value)return null;try{return JSON.parse(value)as AuthUser}catch{return null}}private saveUser(user?:AuthUser){if(user)localStorage.setItem("user",JSON.stringify(user))}isLoggedIn(){const token=this.getToken();if(!token)return false;try{const payload=JSON.parse(atob(token.split(".")[1]))as{exp?:number};return!payload.exp||payload.exp*1000>Date.now()}catch{this.logout();return false}}logout(){localStorage.removeItem(this.tokenKey);localStorage.removeItem("user")}}