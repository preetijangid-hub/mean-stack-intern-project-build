const r=require("express").Router(),a=require("../middleware/auth.middleware"),c=require("../controllers/task.controller");const {validateCreateTask,validateUpdateTask}=require("../validators/task.validator");r.use(a);r.get("/",c.getTasks);r.post("/",validateCreateTask,c.createTask);r.put("/:id",validateUpdateTask,c.updateTask);r.delete("/:id",c.deleteTask);module.exports=r;
